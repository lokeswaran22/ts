# Microsoft Teams / Azure AD Configuration for Frontend
# These variables must be prefixed with VITE_ to be accessible in Vite

# Azure AD Application (client) ID
# Get this from Azure Portal: https://portal.azure.com
VITE_MSAL_CLIENT_ID=your_client_id_here

# Azure AD Directory (tenant) ID
# Get this from Azure Portal
VITE_MSAL_TENANT_ID=your_tenant_id_or_common

# Redirect URI (must match Azure AD app configuration)
VITE_MSAL_REDIRECT_URI=http://localhost:3000

# API Base URL (leave default for local development)
VITE_API_BASE_URL=http://localhost:3000

# Note: If you don't configure these, the app will run in DEMO MODE
# Demo mode allows testing without Microsoft Teams credentials
